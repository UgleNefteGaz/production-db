# Журнал работы

## 09.09.2026

### Выполнено

- выбрана предметная область базы данных;
- определено назначение системы;
- сформирован список из 15 сущностей;
- определены основные атрибуты сущностей;
- определены первичные и внешние ключи;
- спроектированы связи между сущностями;
- подготовлена ER-модель в формате DBML;
- создана структура проекта;
- создан локальный Git-репозиторий;
- создан удалённый репозиторий GitHub;
- настроена связь локального репозитория с GitHub;
- выполнен первый push проекта;
- разработан документ с ограничениями целостности;
- определены правила PRIMARY KEY, FOREIGN KEY, UNIQUE, CHECK, DEFAULT и ON DELETE;
- DBML-модель приведена в соответствие с ограничениями целостности;
- ER-модель проверена в dbdiagram.io;
- подготовлено описание связей и кардинальностей между сущностями;
- разработана физическая схема базы данных для PostgreSQL;
- создан отдельный тестовый контейнер PostgreSQL 18;
- физическая схема успешно развернута в PostgreSQL 18.6;
- создано 15 таблиц и 8 ENUM-типов;
- проверено создание PRIMARY KEY, FOREIGN KEY, UNIQUE, CHECK и NOT NULL;
- проверены правила ON DELETE RESTRICT и ON DELETE CASCADE;
- подготовлен набор связанных тестовых данных;
- тестовые данные успешно загружены во все 15 таблиц;
- проверены связи между таблицами с помощью JOIN;
- проверена цепочка Product -> Specification -> ProductionOrder -> ProductionBatch -> QualityCheck;
- проверен учет производственного брака;
- подтверждена согласованность количества произведенной продукции и зарегистрированного брака.

### Текущий результат

Разработана и проверена рабочая структура базы данных системы учета и контроля производственных процессов предприятия.

База данных содержит 15 сущностей и охватывает:

- продукцию;
- материалы;
- технологические спецификации;
- производственные заказы;
- производственные партии;
- технологические операции;
- производственные линии;
- сотрудников и смены;
- расход материалов;
- контроль качества;
- производственный брак.

Физическая схема успешно проверена на PostgreSQL 18.6.

Для тестирования создан производственный сценарий:

- продукт: Шампунь 500 мл;
- производственный заказ: PO-2026-001;
- две производственные партии;
- технологические операции;
- фактический расход материалов;
- контроль качества;
- зарегистрированные дефекты.

Для партии BATCH-002:

- плановое количество: 500 шт.;
- фактическое количество: 493 шт.;
- зарегистрированный брак: 7 шт.;
- повреждение упаковки: 4 шт.;
- неверная этикетка: 3 шт.

### Следующий этап

- разработать основные SQL-запросы;
- подготовить аналитические запросы по производственным заказам;
- реализовать анализ планового и фактического расхода материалов;
- реализовать отчеты по браку;
- подготовить запросы по производственным линиям и операциям;
- рассмотреть необходимость представлений VIEW;
- после этого перейти к функциям и триггерам.


---

## 15.09.2026

### Выполнено

- все идентификаторы таблиц и полей переведены в формат `lowercase snake_case`;
- схема после рефакторинга полностью пересоздана и проверена в PostgreSQL 18.6;
- подтверждено создание всех 15 таблиц;
- подготовлен анализ предполагаемых запросов к базе данных;
- выполнена оценка кардинальности индексируемых полей;
- разработано 13 дополнительных индексов;
- подготовлено обоснование простых и композитных индексов;
- создан документ `docs/indexes-analysis.md`;
- реализовано 8 дополнительных `CHECK`-ограничений;
- создан частичный `UNIQUE INDEX`, разрешающий только одну активную спецификацию для каждого продукта;
- подготовлен документ `docs/additional-constraints.md`;
- все дополнительные ограничения проверены отрицательными тестами;
- проверена обработка нарушений `CHECK` с SQLSTATE `23514`;
- проверена обработка нарушения уникальности с SQLSTATE `23505`;
- разработаны межтабличные бизнес-правила;
- реализованы триггерные функции на PL/pgSQL;
- создано 6 пользовательских триггеров;
- реализован контроль суммарного планового количества партий относительно производственного заказа;
- реализован запрет уменьшения количества заказа ниже суммы его партий;
- реализован запрет регистрации дефектов для проверки качества с результатом `Passed`;
- реализован запрет установки `Passed` для проверки, имеющей зарегистрированные дефекты;
- реализован контроль времени проверки качества относительно периода производственной партии;
- реализована защита периода партии при наличии существующих проверок качества;
- в триггерных функциях добавлены блокировки `FOR UPDATE` для защиты от конкурентных изменений;
- триггеры проверены отрицательными тестами;
- создан документ `docs/triggers.md`;
- выполнено полное пересоздание базы данных с нуля из файлов проекта;
- подтверждена воспроизводимость схемы, тестовых данных, ограничений, индексов и триггеров.

### Результаты финальной проверки

После полного развёртывания базы данных получено:

```text
tables         = 15
triggers       = 6
batches        = 2
quality_checks = 2
defects        = 2
```

В базе зарегистрированы следующие пользовательские триггеры:

```text
trg_check_batch_planned_quantity
trg_check_batch_quality_check_period
trg_check_defect_quality_result
trg_check_order_planned_quantity
trg_check_quality_check_batch_period
trg_check_quality_passed_without_defects
```

### Реализованные межтабличные правила

1. Сумма `planned_quantity` партий не может превышать `planned_quantity` производственного заказа.
2. Плановое количество заказа нельзя уменьшить ниже уже распределённого по партиям количества.
3. Для проверки качества с результатом `Passed` нельзя зарегистрировать дефекты.
4. Проверку качества нельзя перевести в `Passed`, если для неё уже существуют дефекты.
5. `checked_at` проверки качества не может быть раньше `started_at` производственной партии.
6. Для завершённой партии `checked_at` не может быть позже `completed_at`.
7. Период производственной партии нельзя изменить так, чтобы существующие проверки качества оказались вне него.

### Отложенное бизнес-правило

Не реализован контроль количества дефектов относительно количества продукции, прошедшей контроль качества.

Причина: текущая модель не содержит отдельного однозначного показателя количества продукции, предъявленной на контроль качества.

Реализация правила требует уточнения бизнес-семантики `actual_quantity` либо расширения модели данных.

### Текущий результат

На текущем этапе завершены:

- логическая модель;
- физическая схема PostgreSQL;
- тестовые данные;
- основные ограничения целостности;
- дополнительные бизнес-ограничения;
- индексация;
- межтабличные бизнес-правила;
- триггеры;
- документация по индексам, ограничениям и триггерам;
- проверка полного развёртывания проекта с нуля.

### Следующий этап

- разработать основные SQL-запросы;
- подготовить аналитические запросы;
- реализовать запросы план-факт по производству;
- реализовать анализ расхода материалов;
- реализовать анализ брака и контроля качества;
- подготовить запросы по производственным линиям, операциям и сотрудникам;
- создать необходимые представления `VIEW`;
- определить задачи для пользовательских функций.
