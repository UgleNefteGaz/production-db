# Production DB

Учебный проект реляционной базы данных для учёта и контроля производственных процессов предприятия.

## Назначение проекта

База данных предназначена для моделирования производственного процесса предприятия и позволяет учитывать:

- выпускаемую продукцию;
- сырьё и материалы;
- технологические спецификации;
- производственные заказы;
- производственные партии;
- технологические операции;
- производственные линии;
- сотрудников и производственные смены;
- фактический расход материалов;
- контроль качества;
- производственный брак.

## СУБД

PostgreSQL 18.

Разработка и тестирование выполняются на PostgreSQL 18.6 в отдельном Docker-контейнере.

## Соглашение об именовании

Для таблиц, полей, индексов, ограничений, функций и триггеров используется:

```text
lowercase snake_case
```

Примеры:

```text
production_orders
production_batches
planned_quantity
production_order_id
quality_checks
```

Такой формат позволяет обращаться к объектам PostgreSQL без обязательного использования двойных кавычек.

## Модель данных

База данных содержит 15 основных сущностей:

1. `products`
2. `materials`
3. `specifications`
4. `specification_items`
5. `production_orders`
6. `production_batches`
7. `production_lines`
8. `operations`
9. `batch_operations`
10. `employees`
11. `shifts`
12. `material_usage`
13. `quality_checks`
14. `defect_types`
15. `defects`

Для описания состояний объектов используются 8 ENUM-типов.

ER-модель хранится в формате DBML и может быть визуализирована в dbdiagram.io.

## Структура проекта

```text
production-db/
├── README.md
├── db/
│   ├── schema.dbml
│   ├── schema.sql
│   └── seed.sql
├── docs/
│   ├── requirements.md
│   ├── constraints.md
│   ├── er-diagram.md
│   ├── data-dictionary.md
│   ├── business-tasks.md
│   ├── indexes-analysis.md
│   ├── additional-constraints.md
│   ├── triggers.md
│   └── worklog.md
├── sql/
│   ├── queries.sql
│   ├── views.sql
│   ├── functions.sql
│   ├── triggers.sql
│   ├── indexes.sql
│   └── constraints.sql
└── images/
    └── .gitkeep
```

## Основные файлы

### db/schema.dbml

Логическая модель базы данных в формате DBML.

Содержит:

- таблицы;
- поля;
- типы данных;
- связи;
- первичные и внешние ключи;
- базовые ограничения.

### db/schema.sql

Основная физическая схема PostgreSQL.

Создаёт:

- 8 ENUM-типов;
- 15 таблиц;
- `PRIMARY KEY`;
- `FOREIGN KEY`;
- `UNIQUE`;
- базовые `CHECK`;
- `DEFAULT`;
- правила `ON DELETE`.

### db/seed.sql

Набор связанных тестовых данных для проверки модели.

### sql/indexes.sql

Содержит 13 дополнительных индексов, созданных на основании предполагаемых запросов и кардинальности полей.

### sql/constraints.sql

Содержит дополнительные правила бизнес-логики:

- 8 `CHECK`-ограничений;
- 1 частичный `UNIQUE INDEX`.

### sql/triggers.sql

Содержит 6 пользовательских триггеров для межтабличных бизнес-правил.

## Дополнительные индексы

В проекте создано 13 дополнительных индексов.

Они предназначены для оптимизации:

- поиска производственных заказов по спецификациям;
- выборок заказов и партий по статусу и периоду;
- поиска спецификаций по материалу;
- анализа операций производственных линий;
- анализа операций сотрудников и смен;
- учёта расхода материалов;
- истории проверок качества;
- анализа дефектов.

Подробное обоснование находится в:

```text
docs/indexes-analysis.md
```

## Дополнительные ограничения

Дополнительно реализованы:

```text
8 CHECK
1 частичный UNIQUE INDEX
```

Ограничения контролируют:

- корректность периодов спецификаций;
- наличие только одной активной спецификации продукта;
- плановые даты производственного заказа;
- фактические даты производственной партии;
- обязательные данные завершённых партий;
- фактические даты технологических операций;
- обязательные данные завершённых операций;
- соответствие даты смены времени её начала;
- наличие комментария при проблемном результате контроля качества.

Подробное описание находится в:

```text
docs/additional-constraints.md
```

## Триггеры

В базе реализовано 6 пользовательских триггеров.

Они контролируют:

- суммарное плановое количество партий относительно производственного заказа;
- запрет уменьшения количества заказа ниже суммы его партий;
- запрет дефектов для проверки качества с результатом `Passed`;
- запрет установки `Passed`, если у проверки уже существуют дефекты;
- соответствие времени проверки качества периоду выполнения партии;
- запрет изменения периода партии таким образом, чтобы существующие проверки качества оказались вне него.

Подробное описание находится в:

```text
docs/triggers.md
```

## Тестовые данные

Для проверки модели создан связанный производственный сценарий:

```text
Продукт: Шампунь 500 мл
Заказ: PO-2026-001
Количество партий: 2
```

Для тестового сценария также созданы:

- материалы;
- спецификация;
- производственные линии;
- технологические операции;
- сотрудники;
- смены;
- расход материалов;
- проверки качества;
- типы дефектов;
- зарегистрированные дефекты.

## Проверка проекта

База данных успешно разворачивается с нуля последовательным выполнением:

```text
db/schema.sql
db/seed.sql
sql/constraints.sql
sql/indexes.sql
sql/triggers.sql
```

После полного развёртывания проверено:

```text
Таблиц:              15
Пользовательских триггеров: 6
Тестовых партий:      2
Проверок качества:    2
Зарегистрированных дефектов: 2
```

Дополнительные ограничения и триггеры проверены отрицательными тестами.

Некорректные данные были успешно отклонены PostgreSQL.

## Документация

- `docs/requirements.md` - требования к базе данных;
- `docs/constraints.md` - базовые ограничения целостности;
- `docs/er-diagram.md` - связи и кардинальности;
- `docs/data-dictionary.md` - словарь данных;
- `docs/business-tasks.md` - бизнес-задачи базы данных;
- `docs/indexes-analysis.md` - анализ запросов, кардинальности и индексов;
- `docs/additional-constraints.md` - дополнительные ограничения бизнес-логики;
- `docs/triggers.md` - межтабличные бизнес-правила и триггеры;
- `docs/worklog.md` - журнал разработки проекта.

## Текущее состояние

На текущем этапе завершены:

- логическое проектирование;
- физическое проектирование;
- создание тестовых данных;
- анализ предполагаемых запросов;
- проектирование дополнительных индексов;
- реализация дополнительных ограничений;
- реализация межтабличных бизнес-правил с помощью триггеров;
- проверка полного развёртывания базы данных с нуля.

Следующий этап проекта:

- разработка основных SQL-запросов;
- разработка аналитических запросов;
- создание представлений `VIEW`;
- разработка пользовательских функций.
